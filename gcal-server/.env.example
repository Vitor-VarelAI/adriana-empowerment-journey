# Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Must match what is configured in Google Cloud Console OAuth credentials
# Example local redirect: http://localhost:8083/auth/callback
# Production redirect: https://www.adrianairia.pt/auth/callback
# Canonical env var:
GOOGLE_REDIRECT_URI=http://localhost:8083/auth/callback
# Legacy (deprecated) name still supported for compatibility:
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8083/auth/callback

# The email/account that will be used (Adriana)
ADMIN_EMAIL=adriana@example.com

# Server settings
PORT=3001
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:8081

# Security settings
ENCRYPTION_KEY=your-secure-encryption-key-here-minimum-32-characters
TRUST_PROXY_HOPS=1

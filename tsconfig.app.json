{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "strict": true,
    // noUnusedLocals, noUnusedParameters, noImplicitAny are covered by strict: true
    // noFallthroughCasesInSwitch is not covered by strict: true, so we keep it if desired
    // For this task, I'll assume it should be kept or defaults to a desired value when strict is true.
    // If it was meant to be false, it should be explicitly set.
    // Given it was false, and strict mode doesn't force it true, I'll keep it false.
    "noFallthroughCasesInSwitch": false,

    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
